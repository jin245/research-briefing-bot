# arXiv / Blog data fetching
feedparser>=6.0,<7
requests>=2.28,<3

# Date parsing
python-dateutil>=2.8,<3

# Environment variables (.env)
python-dotenv>=1.0,<2

# Tracking config (config.yml / config.example.yml)
PyYAML>=6.0,<7

# PDF briefing generation (HTML → PDF)
xhtml2pdf>=0.2.11,<1
